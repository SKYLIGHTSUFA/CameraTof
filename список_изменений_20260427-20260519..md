Представление внесённых правок в проект на основе `git diff`.

---

# **Изменения в проекте**

В рамках последнего обновления были внесены изменения в файлы `app.py` и `config.json`.

---

## **app.py**

### 1. **Оптимизация параметров кодирования H.264 (NVENC)** 

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| `-preset p7`     | `-preset p3`   | Уменьшено время кодирования для повышения производительности (330 строчка) |
| `-b:v 800k`      | `-b:v 600k`    | Снижение битрейта для экономии пропускной способности(333 строчка) |
| `-rc vbr`        | `-rc cbr`      | Переключено на постоянный битрейт для стабильной передачи данных(336 строчка) |

---

### 2. **Улучшение параметров захвата видеопотока**

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| `-probesize 32`  | `-probesize 128k` | Увеличено время анализа заголовка для лучшей стабильности захвата(357 строчка) |

---

### 3. **Изменение логики обработки кадров**

| Старое условие | Новое условие | Комментарий |
|----------------|----------------|-------------|
| `now - last_frame_time < frame_interval` | `now - last_frame_time <= frame_interval` | Изменено условие для более точного контроля частоты кадров (613 строчка) |

---

### 4. **Настройка параметров камеры**

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| `AcquisitionFrameRateMode Off` | `AcquisitionFrameRateMode On` | Включён режим управления частотой кадров(183 строчка) |
| `AcquisitionFrameRate 20` | `AcquisitionFrameRate 5` | Установлена более низкая частота кадров для снижения нагрузки (185 строчка) |
| `GevSCPSPacketSize 1500` | `GevSCPSPacketSize 8000` | Увеличена максимальная длина пакета для повышения эффективности передачи данных (226 строчка) |
| `AcquisitionFrameRateEnable False` | Удалено | Удалено устаревшее свойство (227 строчка) |

---

### 5. **Активация потока кодирования**

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| Комментарии | Активация потока | Убраны комментарии, поток теперь активен (не нашел) |

---

## **config.json**

### 1. **Изменение размера кадра и частоты кадров**

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| `"height": 240` | `"height": 300` | Увеличена высота кадра для лучшего качества изображения (5 строчка) |
| `"fps_target": 20` | `"fps_target": 5` | Снижена частота кадров для оптимизации производительности (8 строчка) |


---

### 2. **Корректировка параметров работы с буферами и таймаутами**

| Старое значение | Новое значение | Комментарий |
|------------------|----------------|-------------|
| `"gentl_buffer_count": 32` | `"gentl_buffer_count": 16` | Уменьшено количество буферов для снижения потребления памяти (17 строчка) |
| `"fetch_timeout_s": 2` | `"fetch_timeout_s": 1.5` | Уменьшен таймаут для более быстрой реакции на изменения (18 строчка) |


### 3. **Добавлены камеры с фм91-фм104**

